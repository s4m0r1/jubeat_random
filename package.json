{
  "name": "jubeat_random",
  "version": "1.0.0",
  "description": "My superb Nuxt.js project",
  "author": "s4m0r1",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "build:stg": "cross-env NODE_ENV=\"staging\" nuxt build",
    "build:prod": "cross-env NODE_ENV=\"production\" nuxt build",
    "start": "nuxt start",
    "start:stg": "cross-env NODE_ENV=\"staging\" nuxt start",
    "start:prod": "cross-env NODE_ENV=\"production\" nuxt start",
    "test:stg": "cross-env NODE_ENV=\"staging\" nuxt build && nuxt start",
    "test:prod": "cross-env NODE_ENV=\"production\" nuxt build && nuxt start",
    "generate": "cross-env NODE_ENV=\"production\" nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "@nuxt/typescript-runtime": "^0.2.4",
    "@nuxtjs/axios": "^5.3.6",
    "cross-env": "^5.2.1",
    "express": "^4.16.4",
    "hard-source-webpack-plugin": "^0.13.1",
    "nuxt": "^2.0.0",
    "vue-property-decorator": "^8.3.0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^1.0.1",
    "@nuxtjs/eslint-module": "^1.0.0",
    "@nuxtjs/vuetify": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "cross-env": "^5.2.1",
    "eslint-plugin-nuxt": ">=0.4.2",
    "nodemon": "^1.18.9"
  }
}
